import React, { useState } from "react";
import Form from './Form';





const Main = () => {
  return(
    <main>
      <Form
        // handleSubmit={this.handleSubmit}
      />

    </main>
  )
}

export default Main;